<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ieb.coffee.com.br.issoebrasil">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <permission
        android:name="br.iesb.android.androidmaps.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="br.iesb.android.androidmaps.permission.MAPS_RECEIVE" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity
            android:name=".activity.CadastroActivity"
            android:label="@string/title_activity_cadastro"
            android:parentActivityName=".activity.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ieb.coffee.com.br.issoebrasil.activity.LoginActivity" />
        </activity>
        <activity
            android:name=".activity.DrawerActivity"
            android:label="@string/title_activity_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity.EducacaoActivity"
            android:label="@string/title_activity_educacao"
            android:parentActivityName=".activity.DrawerActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ieb.coffee.com.br.issoebrasil.activity.DrawerActivity" />
        </activity>
        <activity
            android:name=".activity.MapEducacaoActivity"
            android:label="@string/title_activity_map_educacao">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ieb.coffee.com.br.issoebrasil.activity.DrawerActivity" />
        </activity>
        <activity
            android:name=".activity.PerfilActivity"
            android:label="@string/title_activity_perfil"
            android:parentActivityName=".activity.DrawerActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ieb.coffee.com.br.issoebrasil.activity.DrawerActivity" />
        </activity>
        <activity
            android:name=".activity.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".activity.DrawerActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ieb.coffee.com.br.issoebrasil.activity.DrawerActivity" />
        </activity>
        <activity
            android:name=".activity.ConversaActivity"
            android:label="@string/title_activity_conversas"
            android:parentActivityName=".activity.DrawerActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ieb.coffee.com.br.issoebrasil.activity.DrawerActivity" />
        </activity>
        <activity
            android:name=".activity.ConversasActivity"
            android:label="@string/title_activity_conversas"
            android:parentActivityName=".activity.DrawerActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ieb.coffee.com.br.issoebrasil.activity.DrawerActivity" />
        </activity>
        <activity android:name=".activity.ValidadorActivity"></activity>
    </application>

</manifest>